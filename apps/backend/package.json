{
  "name": "@trailwise/backend",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "slack:check": "tsx src/slack-check.ts",
    "simulate": "tsx src/simulate.ts",
    "test": "node --import tsx --test src/*.test.ts"
  },
  "dependencies": {
    "@trailwise/shared": "0.1.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3"
  }
}
